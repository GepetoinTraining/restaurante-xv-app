/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // (Optional) Add image hostnames here if you load from external URLs
  // images: {
  //   remotePatterns: [
  //     {
  //       protocol: 'https',
  //       hostname: 'example.com',
  //     },
  //   ],
  // },
};

export default nextConfig;
